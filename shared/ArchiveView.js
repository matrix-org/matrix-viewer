const {
  TemplateView,
  RoomView,
  RightPanelView
} = require('hydrogen-view-sdk');

export class ArchiveView extends TemplateView {
  render(t, vm) {
    return t.div({
        className: {
            "ArchiveView": true,
        },
    }, [

    ]
  }
}
